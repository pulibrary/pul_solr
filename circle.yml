machine:
  ruby:
    version: 2.3.0
  java:
    version: openjdk8
dependencies:
  bundler:
    without: [development, production]
  post:
    - bundle exec rake solr:start
test:
  override:
    - bundle exec rake spec
